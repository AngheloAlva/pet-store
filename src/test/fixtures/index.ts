/**
 * Fixtures: snapshots of src/data/* used by both the seed script and tests.
 * When src/data/* is eventually deleted, update these imports to pull from
 * a static JSON file or inline the data here.
 */
export { brands } from "@/data/brands";
export { categories } from "@/data/categories";
export { products } from "@/data/products";
export { stores } from "@/data/stores";
export { getStockLevel, stockExceptions } from "@/data/stock";
